{
  "name": "casa-cuvantului-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd server && npm run build && cd ../client && npm run build"
  }
}