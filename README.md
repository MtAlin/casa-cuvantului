# Casa Cuvantului - Bible Study Planner

Full-stack app: React + Node.js + MongoDB

## Setup
1. cd server, npm install, copy .env.example to .env and fill values, npm run dev
2. cd client, npm install, npm run dev
